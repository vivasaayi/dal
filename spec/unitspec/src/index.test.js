"use strict";

const Index = require("../../../index");

describe("Mongo Wrapper", () => {
  it("should export Mongo Wrapper", () => {
    expect(Index.MongoWrapper).toBeDefined();
  });

  it("should export Mongo Config", () => {
    expect(Index.MongoWrapper).toBeDefined();
  });
});